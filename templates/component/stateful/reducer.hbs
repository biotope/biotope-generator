import {Reducer} from 'redux';
import {ComponentAction} from '../../resources/ts/state/ComponentAction';
import {createInitial{{pascalCase name}}State, {{pascalCase name}}State} from "./{{pascalCase name}}.state";

const reducerCreator: (componentId: string) => Reducer = (componentId: string) =>
	(state: {{pascalCase name}}State = createInitial{{pascalCase name}}State(), action: ComponentAction): {{pascalCase name}}State => {
		if (action.id == componentId) {
			switch (action.type) {
				default:
				return state;
			}
		} else {
			return state;
		}
};

export default reducerCreator;
