{
  "name": "@biotope/generator",
  "version": "2.5.0",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "release": "np",
    "prepublishOnly": "npm run clean && npm install && npm run clean && yarn install",
    "clean": "node_modules/.bin/rimraf node_modules",
    "test": "echo \"No tests found\""
  },
  "dependencies": {
    "hygen": "^1.6.2",
    "inquirer": "^6.2.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/biotope/biotope-generator.git"
  },
  "engines": {
    "node": ">=8.9.0",
    "yarn": ">=1.4.0"
  },
  "devDependencies": {
    "np": "^3.0.4",
    "rimraf": "^2.6.2"
  }
}
